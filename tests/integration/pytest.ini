[pytest]
markers =
    mqtt: MQTT broker / transport integration tests
    e2e: End-to-end tests (native_sim DUT + broker)
    gnss: GNSS-related integration tests (reserved)
    hil: tests that require real hardware
filterwarnings =
    ignore::DeprecationWarning
